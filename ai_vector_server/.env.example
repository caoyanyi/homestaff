# AI Vector Server 配置文件示例
# 复制此文件并重命名为 .env 来使用自定义配置

# ======================
# OpenAI API 配置
# ======================

# OpenAI API 密钥
# 默认值: sk-xxxxx (占位符)
AI_API_KEY=sk-xxxxx

# OpenAI API 基础 URL
# 默认值: https://api.openai.com/v1
# 对于本地部署的模型，可以设置为 http://localhost:8000/v1 等
AI_API_URL=https://api.openai.com/v1

# 嵌入模型名称
# 默认值: text-embedding-ada-002
AI_MODEL=text-embedding-ada-002

# ======================
# 向量存储配置
# ======================

# 向量存储类型
# 可选值: memory (内存存储), disk (磁盘持久化存储), milvus (Milvus向量数据库)
# 默认值: memory
VECTOR_STORE_TYPE=disk

# ======================
# 磁盘存储配置 (当 VECTOR_STORE_TYPE=disk 时生效)
# ======================

# FAISS 索引文件路径
# 默认值: data/faiss_index.index
VECTOR_STORE_DISK_INDEX_PATH=data/faiss_index.index

# 文档ID文件路径
# 默认值: data/doc_ids.npy
VECTOR_STORE_DISK_IDS_PATH=data/doc_ids.npy

# 文档文本文件路径
# 默认值: data/doc_texts.txt
VECTOR_STORE_DISK_TEXTS_PATH=data/doc_texts.txt

# ======================
# Milvus 配置 (当 VECTOR_STORE_TYPE=milvus 时生效)
# ======================

# Milvus 服务器主机
# 默认值: localhost
VECTOR_STORE_MILVUS_HOST=localhost

# Milvus 服务器端口
# 默认值: 19530
VECTOR_STORE_MILVUS_PORT=19530

# Milvus 集合名称
# 默认值: vectors
VECTOR_STORE_MILVUS_COLLECTION=vectors

# ======================
# 使用说明
# ======================
# 1. 将此文件复制并重命名为 .env
# 2. 根据您的需求修改配置参数
# 3. 运行 ./run.sh 启动服务或使用以下命令：
#    python server.py --port 9000
#
# 配置优先级：命令行参数 > 环境变量 > 默认配置
#
# 示例：使用磁盘存储启动服务
# 1. 在 .env 文件中设置 VECTOR_STORE_TYPE=disk
# 2. 或使用命令行参数：python server.py --store-type disk
#
# 注意：使用磁盘存储时，请确保数据目录有写权限
#      使用 Milvus 存储时，需要提前部署 Milvus 服务